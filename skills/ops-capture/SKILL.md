---
name: ops-capture
description: Capture a raw item into a unit's feed — items land as files in {todo}/inbox/; enqueue only, never execute; inert without gatekeeper. Triggers on "/ops-capture".
---

Stub — see `plans/2026-08-09-session-ops-v1-spec.md`.
